version: 0.2.4
scripts:
  bootstrap: >+
    npm install

    poetry install --no-root

    apt update && apt install -y openssh-client

    wget https://static.oomol.com/sshexec/v1.0.12/installer.sh --output-document /tmp/installer.sh

    bash +x /tmp/installer.sh

icon: ./icon.png
name: ffmpeg
repository: https://github.com/oomol-flows/ffmpeg.git
displayName: FFmpeg
description: A comprehensive video and audio processing toolkit for OOMOL
  platform, providing a collection of reusable blocks for multimedia
  manipulation powered by FFmpeg.
releaseNotes: add video filter
