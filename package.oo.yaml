version: 0.4.9
scripts:
  bootstrap: >+
    npm install

    poetry install --no-root

    apt update && apt install -y openssh-client

    wget https://static.oomol.com/sshexec/v1.0.13/installer.sh --output-document /tmp/installer.sh

    bash +x /tmp/installer.sh

icon: ./icon.png
name: ffmpeg
repository: https://github.com/oomol-flows/ffmpeg.git
displayName: "%display-name%"
description: "%a-comprehensive-video-and-audio-processing-toolkit-for-oomol-pla%"
dependencies:
  {}
releaseNotes: remove layer ci
ui:
  blocks:
    - add-subtitles
    - add-watermark
    - adjust-audio-quality
    - adjust-volume
    - audio-noise-reduction
    - change-framerate
    - change-speed
    - compress-video
    - convert-audio-format
    - convert-video-format
    - extract-audio
    - extract-video-frames
    - get-media-info
    - make-mkv-video
    - merge-multiple-audio
    - merge-audio-video-subtitle
    - merge-videos
    - remove-audio
    - replace-audio
    - resize-video
    - rotate-video
    - trim-audio
    - trim-video
    - video-filters
    - video-to-gif
